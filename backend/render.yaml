services:
  - type: web
    name: hedera-token-analyzer-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_DIR
        value: /data/token_data
    disk:
      name: token-data
      mountPath: /data
      sizeGB: 1